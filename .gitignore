node_modules/
*.zip
layouts/
Map/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# local tool config — keep locally, do not commit
opencode.json
agent.md
.ignore

# internal AI dev docs — not for the public repo
/docs/
AGENTS.md
CLAUDE.md
**/CLAUDE.md
.opencode/
test-preferences.json
